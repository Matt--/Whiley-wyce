#define LIBRARY_TESTING true

#include "../../cCompiler/mattCompiler.h"
#include "../../cCompiler/mattCompiler_library.c"
Any reverse ( Any );

Any reverse ( Any a0 ){
  Any a1 = ;
  Any a3 = Int(0);
  Any a5 = Int( sizeof( a0 ) / sizeof( a0[0] ) );
  a3, a5};
  int count = a6[0];
  loop_start_label912: ;
  if(count == a6[1]){ goto label912; }
  Any a7 = a6[count];
  count++;
  Any a15 = Int(0);
  Any a16 = Int( sizeof( a0 ) / sizeof( a0[0] ) );
  Any a11 = a0[a7.i];
  Any a13 = a7;
