#define LIBRARY_TESTING true

#include "../../cCompiler/mattCompiler.h"
#include "../../cCompiler/mattCompiler_library.c"
typedef Node;

Any Tree ( Any , Any , Any );

Any Tree ( Any a0, Any a1, Any a2 ){
  if(a12.type == NULL_TYPE){ goto label11; };
  label13: ;
  if( !a12.type == ){ goto label12; };
