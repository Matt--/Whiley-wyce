#define LIBRARY_TESTING true

#include "../../cCompiler/mattCompiler.h"
#include "../../cCompiler/mattCompiler_library.c"
Any meth ( Any[] );

Any meth ( Any a0[] ){
  Any a1 = ;
  Any a3 = Int(0);
  Any a5 = Int( sizeof( a0 ) / sizeof( a0[0] ) );
  a3, a5};
  int count = a6[0];
  loop_start_label1143: ;
  if(count == a6[1]){ goto label1143; }
  Any a7 = a6[count];
  count++;
  Any a16 = Int(0);
  Any a17 = Int( sizeof( a0 ) / sizeof( a0[0] ) );
  Any a12 = a0[a7.i];
  Any a9 = toUnsignedInt ( a12 );
  Any a13[] = {a9};
